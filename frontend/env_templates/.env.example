# .env.example for Testudo Frontend
#
# This file serves as a template for environment variables used by the Testudo frontend.
# It should be copied to .env and populated with actual values for local development.
#
# Purpose: Provide a clear structure for required environment variables.
#
# TODO: Senior Implementation - Add more specific environment variables as needed.

# Frontend API URL
# This is the URL where the frontend will make API requests to the backend.
# Example: http://localhost:8000
VITE_API_URL=http://localhost:8000

# Authentication Server URL
# The URL of the OIDC authentication server (e.g., Keycloak).
# Example: http://localhost:8080/realms/testudo
OIDC_AUTH_SERVER_URL=

# OIDC Client ID
# The client ID for the frontend application registered with the OIDC provider.
OIDC_CLIENT_ID=

# OIDC Redirect URI
# The URI where the authentication server will redirect after successful login.
# Example: http://localhost:8080/auth/callback
OIDC_REDIRECT_URI=

# Static IP Address for Binance Whitelisting
# This IP address should be displayed to the user during onboarding for API key whitelisting.
# TODO: Senior Implementation - Replace with actual static IP.
STATIC_BINANCE_IP_ADDRESS=192.168.1.100
