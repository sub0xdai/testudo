# cargo audit exception policy
# Generated by CLN-01-dependency-hardening CP-2 (2026-05-17)
#
# All allowed advisories are transitive through alloy 0.1.4 (pinned by
# hyperliquid-sdk-rs@0.1.2) or sqlx-mysql (no upstream fix).
# Full resolution requires upgrading hyperliquid-sdk-rs to alloy >=0.8.26,
# which is tracked separately.

[advisories]
ignore = [
    # alloy-dyn-abi 0.7.7: DoS on TypedData hashing (CVSS 7.5)
    # Blocked by hyperliquid-sdk-rs@0.1.2 which pins alloy 0.1.4.
    # Mitigation: TypedData hashing is not used in the Hyperliquid API path;
    # only Address and PrivateKeySigner primitives are used.
    # Resolution requires hyperliquid-sdk-rs to support alloy >=0.8.
    "RUSTSEC-2025-0073",

    # rsa 0.9.10: Marvin Attack (CVSS 5.9, MEDIUM)
    # Transitive through sqlx-mysql. No fix is available upstream.
    # Mitigation: MySQL connections are to internal PostgreSQL-equivalent
    # via sqlx — the rsa dependency is unused at runtime for our config.
    "RUSTSEC-2023-0071",

    # derivative 2.2.0: unmaintained (transitive via alloy 0.1.4/0.7)
    # Resolved when alloy is upgraded to >=0.8.
    "RUSTSEC-2024-0388",

    # paste 1.0.15: unmaintained (transitive via alloy 0.7)
    # Resolved when alloy is upgraded to >=0.8.
    "RUSTSEC-2024-0436",

    # proc-macro-error 1.0.4: unmaintained (transitive via alloy 0.7)
    # Resolved when alloy is upgraded to >=0.8.
    "RUSTSEC-2024-0370",

    # lru 0.12.5: unsound IterMut (transitive via alloy-provider 0.1.4)
    # IterMut is not used in our code paths. Resolved when alloy upgraded.
    "RUSTSEC-2026-0002",
]
